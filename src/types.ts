export interface TimelineEvent {
  year: string;
  title: string;
  description: string;
}

export interface LegacyItem {
  title: string;
  description: string;
  icon?: string;
}

export interface Quote {
  text: string;
  author: string;
}